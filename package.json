{
  "name": "privguard",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source Privacy Compliance & Data Governance Platform",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @privguard/api dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  },
  "packageManager": "pnpm@8.14.0"
}
